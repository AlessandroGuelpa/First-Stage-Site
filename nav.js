// quando clicchi sul burger
function funzione() {
  // salva in una variabile la classe work
  let nav = document.querySelector(".work");
  // aggiungi alla classe ".work" la classe ".open" che aprirà il menù
  nav.classList.toggle("open");
}
